fn main() {
    tracing_subscriber::fmt::init();
    tracing::info!("sagashield: transactional Saga runtime — usa `cargo test` per le demo");
    println!("sagashield: transactional Saga runtime — usa `cargo test` per le demo");
}
